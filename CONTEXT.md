# 多租户 CRM 平台领域语言

本文件规定平台与首家公司模板共用的业务术语。产品文档、页面文案、API 和代码命名应保持一致。

## 平台与身份

**租户（Tenant）**：
使用平台的独立企业工作空间，也是数据隔离的边界。
_避免：公司（用于数据库和权限边界时）、客户公司、站点_

**平台管理员（Platform Admin）**：
运营整个平台、创建租户并为租户配置能力的内部人员。
_避免：超级用户、最高管理员、老板_

**租户成员（Tenant Member）**：
一个用户在某个租户中的业务身份，包含租户角色和启停状态。
_避免：公司用户、员工账号、tenant user_

**租户管理员（Tenant Admin）**：
可查看和管理本租户全部业务数据与成员的租户成员。
_避免：公司大管理员、企业管理员、店长_

**员工（Employee）**：
只能访问获准模块，并在授权数据范围内处理业务的租户成员。
_避免：普通用户、填表人_

**集成身份（Integration Identity）**：
代表飞书机器人、电话 Bot 或外部系统调用平台 API 的非人类身份。
_避免：机器人用户、虚拟员工_

## 配置与数据

**业务对象（Business Object）**：
一类可配置的业务数据，例如获客、跟单、客户或期刊。
_避免：表、Excel、数据库表、模块（指数据类型时）_

**字段定义（Field Definition）**：
业务对象中一个字段的名称、类型、校验、选项和权限配置。
_避免：列配置、数据库字段_

**业务记录（Record）**：
属于某个业务对象的一条租户数据，并具有负责人、状态和生命周期。
_避免：表单、行数据、Excel 行_

**记录负责人（Record Owner）**：
对业务记录承担日常处理责任的租户成员，也是员工 OWN 数据范围的判定依据。
_避免：创建人、录入人、所属员工_

**记录活动（Record Activity）**：
附属于业务记录、按时间追加且不可覆盖历史的业务动作，例如电话沟通或发送报价。
_避免：第 N 次跟进字段、备注列_

**业务模板（Business Template）**：
可复用的业务对象、字段、视图、状态和权限初始配置集合。
_避免：一套代码、独立 CRM、复制站点_

**租户配置（Tenant Configuration）**：
业务模板应用到租户后形成、可继续调整的专属配置。
_避免：模板副本（当它暗示代码复制时）_

**视图（View）**：
对同一业务对象记录进行筛选、排序和列展示后的工作入口。
_避免：员工表、管理员总表、独立数据表_

## 首家公司业务

**线索（Lead）**：
通过电话 Bot 或人工外呼获得、尚未进入正式跟单的潜在业务机会。
_避免：潜客、获客记录、名单（进入系统后）_

**线索审核（Lead Review）**：
员工判断电话 Bot 结果是否有效、是否应转入跟单的业务动作。
_避免：分配审核、客户审核_

**跟单（Opportunity）**：
已确认有效并处于成交前持续推进阶段的业务机会。
_避免：跟进表、商机（界面首期）、订单_

**客户（Customer）**：
跟单成功后正式接受服务的个人或组织。
_避免：线索、联系人、成交记录_

**期刊记录（Journal Record）**：
首家公司用于登记期刊业务数据并由管理员完成确认与评级的记录。
_避免：期刊表（指单条数据时）_
