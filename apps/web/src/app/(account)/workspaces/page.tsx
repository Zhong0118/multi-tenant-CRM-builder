import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function WorkspacesPage() {
  return (
    <PagePlaceholder
      title="选择工作空间"
      description="选择当前用户已经加入的公司。"
    />
  );
}
